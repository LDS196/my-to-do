import React from 'react';

type ButtonType = {

}
const Button: React.FC<ButtonType> = () => {
    return (
        <div>
            <button></button>
        </div>
    );
};

export default Button;